# slogpy Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-06

## Active Technologies

- Python ≥ 3.10 (currently `^3.8`, changing to `>=3.10`) + Rich (`>=12,<15`, currently `^13.7.0`) (001-idiomatic-naming-deps)

## Project Structure

```text
slogpy/
tests/
```

## Commands

```bash
# Install dependencies
poetry install

# Run tests
poetry run pytest

# Lint
poetry run ruff check .
```

## Code Style

Python ≥ 3.10 (currently `^3.8`, changing to `>=3.10`): Follow standard conventions

## Recent Changes

- 001-idiomatic-naming-deps: Added Python ≥ 3.10 (currently `^3.8`, changing to `>=3.10`) + Rich (`>=12,<15`, currently `^13.7.0`)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
